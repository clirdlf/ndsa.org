{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Start Jekyll",
            "type": "shell",
            "command": "bundle exec jekyll serve -l --host localhost --port 3000",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}